// ES Module wrapper for textCorrector to use in React components
import textCorrectorModule from './textCorrector.js';

// Extract the textCorrector instance
const { textCorrector } = textCorrectorModule;

export { textCorrector };
